# iam-least-privilege-auditor
Python tool that scans AWS IAM users, groups, and rles for over-permissive policies (wildcard actions/rsources, risky permissions) and produces a severity-anked findings report.
